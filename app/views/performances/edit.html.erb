<h2><%= @metric.name %></h2>

<%= form_for [current_user,@performance.metric, @performance] do |f|  %>
  <%= f.label :count %>
  <%= f.number_field :count, {:min=>"0"} %>

  <%= f.label :date %>
  <%= f.date_field :date, :default => Time.current.to_date%>

  <%= f.submit "Enter" %>

<% end %>
